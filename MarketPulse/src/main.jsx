import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{AreaChart,Area,ResponsiveContainer,PieChart,Pie,Cell}from'recharts';import'./styles.css';
const api=async(p,o={})=>{let r=await fetch('/api'+p,{...o,headers:{'Content-Type':'application/json',...(localStorage.mp_token?{Authorization:'Bearer '+localStorage.mp_token}:{}),...o.headers}}),b=r.status===204?null:await r.json();if(!r.ok)throw Error(b.error);return b},nav=[['Dashboard','⌂'],['Watchlist','★'],['Explore','⌕'],['News','▣'],['Alerts','♧'],['Portfolio','▥'],['Insights','✦'],['Demo Mode','◉'],['Settings','⚙']],cls={"HIGH ATTENTION":'high',"WORTH CHECKING":'watch',STABLE:'stable'};
const Badge=({s})=><span className={'badge '+cls[s]}><i/> {s==='HIGH ATTENTION'?'High attention':s==='WORTH CHECKING'?'Worth checking':'Stable'}</span>;
function Auth({done}){let[signup,setSignup]=useState(false),[f,setF]=useState({name:'Khushi',email:'',password:''}),[e,setE]=useState('');let submit=async x=>{x.preventDefault();try{let d=await api('/auth/'+(signup?'register':'login'),{method:'POST',body:JSON.stringify(f)});localStorage.mp_token=d.token;done(d.user)}catch(x){setE(x.message)}};return <div className="login"><section><div className="brand">market<span>pulse</span></div><p className="kicker">INVESTOR CLARITY, NOT NOISE</p><h1>Know what changed.<br/><em>Know why it matters.</em></h1><p>MarketPulse turns your watchlist into a personal briefing with context.</p></section><form onSubmit={submit}><p className="kicker">WELCOME TO MARKETPULSE</p><h2>{signup?'Create your account':'Welcome back'}</h2>{signup&&<input placeholder="Name" value={f.name} onChange={x=>setF({...f,name:x.target.value})}/>}<input type="email" required placeholder="Email" onChange={x=>setF({...f,email:x.target.value})}/><input type="password" required minLength="6" placeholder="Password" onChange={x=>setF({...f,password:x.target.value})}/>{e&&<p className="error">{e}</p>}<button className="primary">{signup?'Create account':'Sign in to MarketPulse'}</button><p>{signup?'Already registered?':'New here?'} <button type="button" onClick={()=>setSignup(!signup)}>{signup?'Sign in':'Create account'}</button></p></form></div>}
function Shell({page,setPage,user,logout,children}){return <div className="app"><aside><div className="brand">market<span>pulse</span></div><nav>{nav.map(([n,i])=><button className={page===n?'active':''} onClick={()=>setPage(n)} key={n}><b>{i}</b>{n}</button>)}</nav><div className="sideuser"><i>{user.name[0]}</i><span><b>{user.name}</b><small>Free plan</small></span><button onClick={logout}>↪</button></div></aside><header><div className="brand">market<span>pulse</span></div><button onClick={logout}>Sign out</button></header><main>{children}</main><footer>{nav.slice(0,6).map(([n,i])=><button onClick={()=>setPage(n)} className={page===n?'active':''} key={n}>{i}<small>{n}</small></button>)}</footer></div>}
const Title=({title,sub,children})=><div className="title"><div><p className="kicker">MARKETPULSE</p><h1>{title}</h1><p>{sub}</p></div>{children}</div>;const Empty=()=> <div className="empty"><b>Your watchlist is empty.</b><p>Add stocks and MarketPulse will tell you what changes.</p></div>;
function Dashboard({d,open}){let top=[...d.items].sort((a,b)=>b.score-a.score).slice(0,5),events=top.flatMap(x=>x.news.map(n=>({...n,symbol:x.symbol}))).slice(0,2);return <><Title title="Good morning, Khushi 👋" sub="Here’s what changed since your last visit."/><div className="market"><i/> <b>US Market Open</b><span>S&P 500 <strong className="up">+0.42%</strong></span><span>NASDAQ <strong className="up">+0.68%</strong></span><small>Updated just now</small></div><section className="stats">{[[d.counts['HIGH ATTENTION'],'Need attention','red','↗'],[d.counts['WORTH CHECKING'],'Worth checking','orange','◌'],[d.counts.STABLE,'Stable','green','✓']].map(x=><article className={x[2]} key={x[1]}><div><small>{x[1].toUpperCase()}</small><b>{x[0]}</b><span>stocks in your watchlist</span></div><i>{x[3]}</i></article>)}</section><div className="section"><div><p className="kicker">YOUR WATCHLIST, DISTILLED</p><h2>Since you last checked</h2></div><small>Showing the most important changes</small></div><section className="signals">{top.length?top.map(x=><button key={x.symbol} onClick={()=>open(x.symbol)}><i>{x.symbol[0]}</i><span><b>{x.symbol}</b><small>{x.companyName}</small></span><span><small>SINCE LAST CHECK</small><b className={x.priceChange>=0?'up':'down'}>{x.priceChange>=0?'+':''}{x.priceChange}%</b></span><span><small>VOLUME</small><b>{x.volumeRatio}× normal</b></span><Badge s={x.status}/><em>→</em></button>):<Empty/>}</section><div className="two"><Overview/><section className="events"><div className="section"><h2>Recent important events</h2><button>View news →</button></div>{events.map(e=><button onClick={()=>open(e.symbol)} key={e.title}><i>✦</i><span><b>{e.title}</b><small>{e.symbol} · {e.source}</small></span><em>High impact</em></button>)}</section></div></>};function Overview(){let d=[4,7,6,10,9,13,15].map((p,i)=>({i,p}));return <section className="overview"><p className="kicker">MARKET OVERVIEW</p><b>S&P 500 <span>5,282.14 <strong className="up">+0.42%</strong></span></b><ResponsiveContainer width="100%" height={110}><AreaChart data={d}><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop stopColor="#40dfbb" stopOpacity=".5"/><stop offset="1" stopColor="#40dfbb" stopOpacity="0"/></linearGradient></defs><Area dataKey="p" stroke="#35caaa" fill="url(#g)"/></AreaChart></ResponsiveContainer></section>}
function Watchlist({d,open,reload,toast}){let[q,setQ]=useState(''),[f,setF]=useState('All'),[r,setR]=useState([]),items=d.items.filter(x=>(f==='All'||x.status===f)&&(`${x.symbol} ${x.companyName}`.toLowerCase().includes(q.toLowerCase())));let find=async v=>{setQ(v);setR(v?await api('/watchlist/search?q='+v):[])};return <><Title title="My Watchlist" sub="Keep the companies you care about in focus."/><div className="controls"><input value={q} onChange={e=>find(e.target.value)} placeholder="Search or add a stock"/><select value={f} onChange={e=>setF(e.target.value)}><option>All</option><option>HIGH ATTENTION</option><option>WORTH CHECKING</option><option>STABLE</option></select><div className="suggest">{r.map(x=><button key={x.symbol} onClick={async()=>{try{await api('/watchlist',{method:'POST',body:JSON.stringify({symbol:x.symbol})});toast('Stock added');reload()}catch(e){toast(e.message)}}}><b>{x.symbol}</b>{x.companyName}<em>＋</em></button>)}</div></div><section className="table"><div className="row head"><span>Stock</span><span>Current price</span><span>Today</span><span>Since last check</span><span>Volume</span><span>Attention</span><span/></div>{items.map(x=><div className="row" key={x.symbol}><button className="co" onClick={()=>open(x.symbol)}><i>{x.symbol[0]}</i><span><b>{x.symbol}</b><small>{x.companyName}</small></span></button><span>${x.price}</span><span className={x.dayChange>=0?'up':'down'}>{x.dayChange>=0?'+':''}{x.dayChange}%</span><span className={x.priceChange>=0?'up':'down'}>{x.priceChange>=0?'+':''}{x.priceChange}%</span><span>{x.volumeRatio}×</span><Badge s={x.status}/><button onClick={async()=>{if(confirm('Remove '+x.symbol+'?')){await api('/watchlist/'+x.symbol,{method:'DELETE'});reload();toast('Stock removed')}}}>⋮</button></div>)}</section></>}
function Detail({x,back}){return <><button className="back" onClick={back}>← Back to briefing</button><div className="detailhead"><div><p className="kicker">{x.companyName}</p><h1>{x.symbol} <Badge s={x.status}/></h1><b className="price">${x.price} <span className={x.dayChange>=0?'up':'down'}>{x.dayChange>=0?'+':''}{x.dayChange}% today</span></b></div><div className="ranges"><button>1D</button><button className="on">1W</button><button>1M</button><button>3M</button><button>1Y</button></div></div><section className="bigchart"><ResponsiveContainer width="100%" height={280}><AreaChart data={x.history}><defs><linearGradient id="d" x1="0" y1="0" x2="0" y2="1"><stop stopColor="#40dfbb" stopOpacity=".45"/><stop offset="1" stopColor="#40dfbb" stopOpacity="0"/></linearGradient></defs><Area dataKey="price" stroke="#34ccaa" fill="url(#d)" strokeWidth={3}/></AreaChart></ResponsiveContainer></section><section className="detailcards"><article><p className="kicker">SINCE YOU LAST CHECKED</p><div><span><small>PRICE</small><b>${x.previousPrice} → ${x.price}</b><strong className={x.priceChange>=0?'up':'down'}>{x.priceChange>=0?'+':''}{x.priceChange}%</strong></span><span><small>VOLUME</small><b>{x.volumeRatio}× normal</b></span><span><small>NEWS</small><b>{x.news.length} important events</b></span></div><Badge s={x.status}/></article><article className="care"><p className="kicker">WHY YOU SHOULD CARE</p><h3>{x.why}</h3><small>MarketPulse provides context, not investment advice.</small></article></section><section className="events"><p className="kicker">RECENT NEWS & COMPANY EVENTS</p>{x.news.length?x.news.map(n=><article key={n.title}><i>✦</i><span><b>{n.title}</b><small>{n.description}</small></span></article>):<p className="muted">No important events detected.</p>}</section></>}
function Explore({toast,reload}){let[q,setQ]=useState(''),[r,setR]=useState([]);let s=async v=>{setQ(v);setR(v?await api('/watchlist/search?q='+v):[])};return <><Title title="Explore markets" sub="Discover the signals shaping today’s market."/><div className="controls"><input value={q} onChange={e=>s(e.target.value)} placeholder="Search a ticker or company"/></div><section className="explore">{[['Trending stocks',['NVDA','AAPL','MSFT']],['Top gainers',['NVDA','AMZN','AAPL']],['Most active',['TSLA','NVDA','AAPL']]].map(([h,a])=><article key={h}><p className="kicker">{h.toUpperCase()}</p>{a.map((s,i)=><button onClick={async()=>{try{await api('/watchlist',{method:'POST',body:JSON.stringify({symbol:s})});toast(s+' added');reload()}catch(e){toast(e.message)}}} key={s}><b>{s}</b><span className="up">+{(4.8-i*1.2).toFixed(1)}%</span><em>＋</em></button>)}</article>)}</section><div className="searchcards">{r.map(x=><button key={x.symbol} onClick={async()=>{await api('/watchlist',{method:'POST',body:JSON.stringify({symbol:x.symbol})});reload();toast(x.symbol+' added')}}><b>{x.symbol}</b><span>{x.companyName}</span>＋</button>)}</div></>}
function News({d,open}){let n=d.items.flatMap(x=>x.news.map(a=>({...a,symbol:x.symbol})));return <><Title title="Market news" sub="Important events, filtered for context."/><div className="tabs"><button className="on">All</button><button>My Watchlist</button><button>Technology</button><button>Finance</button><button>Markets</button></div><section className="newsgrid">{n.length?n.map(x=><button key={x.title} onClick={()=>open(x.symbol)}><span className="newsicon">✦</span><small>{x.symbol} · HIGH IMPACT</small><h3>{x.title}</h3><p>{x.description}</p><footer>{x.source} · 2h ago <b>→</b></footer></button>):<Empty/>}</section></>}
function Alerts({d,toast}){let[a,setA]=useState([]),[show,setShow]=useState(false),[f,setF]=useState({symbol:d.items[0]?.symbol||'NVDA',type:'Price movement',threshold:5});let load=()=>api('/alerts').then(setA);useEffect(load,[]);return <><Title title="Alerts" sub="Be notified when a market moment deserves your attention."><button className="primary" onClick={()=>setShow(true)}>+ Create alert</button></Title><section className="alertlist">{a.length?a.map(x=><article key={x.id}><i>♧</i><div><b>{x.symbol}</b><p>Alert when {x.type.toLowerCase()} moves more than {x.threshold}%</p><small>{x.enabled?'Active':'Paused'}</small></div><label className="switch"><input type="checkbox" checked={x.enabled} onChange={async e=>{await api('/alerts/'+x.id,{method:'PATCH',body:JSON.stringify({enabled:e.target.checked})});load()}}/><i/></label><button onClick={async()=>{await api('/alerts/'+x.id,{method:'DELETE'});load()}}>×</button></article>):<Empty/>}</section>{show&&<div className="modal"><form onSubmit={async e=>{e.preventDefault();await api('/alerts',{method:'POST',body:JSON.stringify(f)});setShow(false);load();toast('Alert created')}}><button type="button" className="close" onClick={()=>setShow(false)}>×</button><p className="kicker">NEW ALERT</p><h2>Create an alert</h2><label>Stock<select value={f.symbol} onChange={e=>setF({...f,symbol:e.target.value})}>{d.items.map(x=><option key={x.symbol}>{x.symbol}</option>)}</select></label><label>Alert type<select onChange={e=>setF({...f,type:e.target.value})}><option>Price movement</option><option>Volume spike</option><option>News event</option></select></label><label>Threshold<input type="number" value={f.threshold} onChange={e=>setF({...f,threshold:e.target.value})}/></label><button className="primary">Save alert</button></form></div>}</>}
function Portfolio({toast}){let[p,setP]=useState(null),[show,setShow]=useState(false),[f,setF]=useState({symbol:'NVDA',shares:1,averagePrice:170});let load=()=>api('/portfolio').then(setP);useEffect(load,[]);if(!p)return null;return <><Title title="Portfolio" sub="A simple view of your manually tracked holdings."><button className="primary" onClick={()=>setShow(true)}>+ Add holding</button></Title><section className="stats portfolio"><article><small>TOTAL PORTFOLIO VALUE</small><b>${p.total.toLocaleString()}</b></article><article><small>OVERALL RETURN</small><b className={p.gain>=0?'up':'down'}>{p.gain>=0?'+':''}${p.gain.toLocaleString()}</b></article><article><small>HOLDINGS</small><b>{p.holdings.length}</b></article></section><section className="table"><div className="row head"><span>Stock</span><span>Shares</span><span>Average price</span><span>Current price</span><span>Gain / Loss</span></div>{p.holdings.map(x=><div className="row" key={x.id}><span><b>{x.symbol}</b></span><span>{x.shares}</span><span>${x.averagePrice}</span><span>${x.currentPrice}</span><span className={x.gain>=0?'up':'down'}>{x.gain>=0?'+':''}${x.gain}</span></div>)}</section>{show&&<div className="modal"><form onSubmit={async e=>{e.preventDefault();await api('/portfolio',{method:'POST',body:JSON.stringify(f)});setShow(false);load();toast('Holding added')}}><button type="button" className="close" onClick={()=>setShow(false)}>×</button><h2>Add holding</h2><input placeholder="Ticker" value={f.symbol} onChange={e=>setF({...f,symbol:e.target.value})}/><input type="number" placeholder="Shares" value={f.shares} onChange={e=>setF({...f,shares:e.target.value})}/><input type="number" placeholder="Average price" value={f.averagePrice} onChange={e=>setF({...f,averagePrice:e.target.value})}/><button className="primary">Save holding</button></form></div>}</>}
function Insights({d}){let h=d.counts['HIGH ATTENTION'],up=d.items.filter(x=>x.priceChange>3).length,news=d.items.sort((a,b)=>b.news.length-a.news.length)[0];return <><Title title="Insights" sub="A calmer view of patterns in your watchlist."/><section className="insights"><article><i>⚡</i><div><p className="kicker">VOLUME SPIKE</p><h3>{d.items.sort((a,b)=>b.volumeRatio-a.volumeRatio)[0]?.symbol||'—'} is trading at {d.items.sort((a,b)=>b.volumeRatio-a.volumeRatio)[0]?.volumeRatio||0}× normal volume.</h3></div></article><article><i>↗</i><div><p className="kicker">MOMENTUM</p><h3>{up} stocks in your watchlist have significant upward movement.</h3></div></article><article><i>✦</i><div><p className="kicker">NEWS ACTIVITY</p><h3>{news?.symbol||'—'} has the highest news activity in your watchlist.</h3></div></article><article><i>⚠</i><div><p className="kicker">WATCHLIST HEALTH</p><h3>{h} stocks need attention. {d.counts.STABLE} are stable.</h3></div></article></section></>}
function Demo({reload,toast}){let scenarios=[['rise','Significant Price Rise','Simulate a sharp price move with elevated volume and news.'],['fall','Significant Price Drop','Show a significant negative market movement.'],['volume','Volume Spike','Simulate unusual trading activity.'],['news','Breaking News','Inject an important company event.'],['calm','No Meaningful Changes','Return to a calm, stable market.'],['outage','API Failure / Stale Data','Use a last-known value and surface a data warning.']];return <><Title title="MarketPulse Demo" sub="Simulate market events and see how MarketPulse responds."/><section className="scenario">{scenarios.map(x=><article key={x[0]}><i>◉</i><div><b>{x[1]}</b><p>{x[2]}</p></div><button className="outline" onClick={async()=>{await api('/demo/scenario',{method:'POST',body:JSON.stringify({scenario:x[0]})});await reload();toast(x[1]+' is now active')}}>Run scenario</button></article>)}</section></>}
function Settings({user,toast}){let[p,setP]=useState({notifications:true,darkMode:true,demoMode:true});useEffect(()=>{api('/preferences').then(setP)},[]);let save=async x=>{let n={...p,...x};setP(n);await api('/preferences',{method:'PATCH',body:JSON.stringify(n)});toast('Settings saved')};return <><Title title="Settings" sub="Manage your MarketPulse experience."/><section className="settings"><article><p className="kicker">PROFILE</p><label>Name<input defaultValue={user.name}/></label><label>Email<input defaultValue={user.email}/></label><button className="outline" onClick={()=>toast('Profile changes are ready for a server-side profile update.')}>Save profile</button></article><article><p className="kicker">PREFERENCES</p>{[['notifications','Notifications','Receive alert and event notifications'],['darkMode','Dark interface','Use the premium dark navigation theme'],['demoMode','Demo mode','Show hackathon scenario controls']].map(([k,h,s])=><div className="pref" key={k}><span><b>{h}</b><small>{s}</small></span><label className="switch"><input type="checkbox" checked={p[k]} onChange={e=>save({[k]:e.target.checked})}/><i/></label></div>)}</article></section></>}
function App(){let[user,setUser]=useState(()=>{try{return JSON.parse(localStorage.mp_user)}catch{return null}}),[page,setPage]=useState('Dashboard'),[d,setD]=useState(null),[detail,setDetail]=useState(null),[toast,setToast]=useState('');let reload=async()=>{try{setD(await api('/changes'))}catch(e){setToast(e.message)}};useEffect(()=>{if(user)reload()},[user]);let open=async s=>{setDetail(await api('/changes/'+s));setPage('Detail')};if(!user)return <Auth done={u=>{localStorage.mp_user=JSON.stringify(u);setUser(u)}}/>;if(!d)return <div className="loading">Loading your briefing…</div>;let content=detail?<Detail x={detail} back={()=>{setDetail(null);setPage('Dashboard')}}/>:page==='Dashboard'?<Dashboard d={d} open={open}/>:page==='Watchlist'?<Watchlist d={d} open={open} reload={reload} toast={setToast}/>:page==='Explore'?<Explore reload={reload} toast={setToast}/>:page==='News'?<News d={d} open={open}/>:page==='Alerts'?<Alerts d={d} toast={setToast}/>:page==='Portfolio'?<Portfolio toast={setToast}/>:page==='Insights'?<Insights d={d}/>:page==='Demo Mode'?<Demo reload={reload} toast={setToast}/>:<Settings user={user} toast={setToast}/>;return <Shell page={page} setPage={p=>{setDetail(null);setPage(p)}} user={user} logout={()=>{localStorage.clear();setUser(null)}}>{content}{toast&&<div className="toast">{toast}<button onClick={()=>setToast('')}>×</button></div>}</Shell>};createRoot(document.getElementById('root')).render(<App/>);
